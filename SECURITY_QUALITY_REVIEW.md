# LayerRenamer 安全与质量审计报告

## 1. 项目熟悉与架构概览

### 1.1 代码组成
- `LayerRenamer_PS-2023.jsx`：Photoshop ExtendScript 脚本，提供 UI 输入并批量重命名图层，支持设置图层颜色。
- `LayerRenamer_AE-2023.jsx`：After Effects ExtendScript 脚本，提供 UI 输入并批量重命名选中图层，支持颜色标签设置。
- `README.md`：项目说明（当前内容较简略）。

### 1.2 执行模型与风险边界
- 两个脚本均运行于 Adobe Host（PS / AE）本地环境，不涉及网络服务、数据库访问、后端 API。
- 安全面主要聚焦于：
  - 输入校验与边界条件控制（防止脚本崩溃、误操作）；
  - 对宿主对象访问的健壮性（空对象、错误上下文、重复名称对象选择错误）；
  - 错误处理与回滚能力（Undo Group、异常提示）；
  - 可维护性与可扩展性（函数分层、常量、注释和规范）。

> 说明：SQL 注入、服务端权限绕过、传输层加密等典型 Web/后端场景风险，在该离线脚本项目中不构成直接攻击面，但仍在“适用性说明”中给出结论。

---

## 2. 系统性安全检测结果

### 2.1 输入验证（高优先级）

#### 发现问题
1. **PS 脚本输入验证不充分**：
   - `baseName` 仅判空，缺少 trim、控制字符清理、长度限制；
   - `numberFormat` 未限制格式，可能输入非数字或过长字符串；
   - 未限制 `startNumber` 范围。  
2. **AE 脚本前缀校验较弱**：
   - 未处理换行/制表符等控制字符；
   - 未限制前缀长度。  

#### 风险说明
- 可能导致运行时异常、生成不可读图层名、超长名称失败、UI 操作不可预期。

#### 已实施修复
- PS：新增 `sanitizeBaseName` + `isValidInput`，限制编号格式为连续 `0`，限制长度和起始范围。
- AE：新增 `sanitizePrefix`，限制前缀长度并移除控制字符。

### 2.2 权限控制（适用性说明）

#### 评估结论
- 该项目为本地脚本，不包含多用户鉴权体系，也无角色权限模型。
- 实际“权限控制”依赖 Adobe 应用本身与用户手动执行权限。

#### 建议
- 在脚本入口增加上下文校验（例如 AE 必须在 `CompItem` 环境执行），避免越权式误调用（错误上下文中访问对象）。

### 2.3 数据加密（适用性说明）

#### 评估结论
- 当前代码不处理用户凭证、密钥或网络传输数据，传输加密（TLS）不适用。
- 若未来增加配置持久化或远程同步，应引入：最小化敏感数据存储 + 加密存储 + 密钥托管策略。

### 2.4 SQL 注入防护（不适用）

#### 评估结论
- 项目无数据库访问与 SQL 语句构造，不存在 SQL 注入攻击面。

### 2.5 XSS 攻击防护（低相关）

#### 评估结论
- 脚本 UI 为宿主原生 UI，不渲染 HTML，不存在典型浏览器 XSS 面。
- 但仍需做输入清理，防止控制字符导致 UI 文本显示异常或脚本逻辑异常（已修复）。

### 2.6 稳定性与错误恢复

#### 发现问题
1. AE 脚本未校验是否有活动合成，直接读取 `selectedLayers` 可能抛异常。  
2. PS/AE 的批处理操作缺少统一异常捕获逻辑，用户难以定位错误。  

#### 已实施修复
- AE：新增 `CompItem` 上下文检查；增加 `try/finally` 确保 `endUndoGroup` 必执行。
- PS：在执行入口增加 `try/catch`，将异常反馈为可读提示。

---

## 3. 代码质量评估与改进建议

### 3.1 代码结构优化
- **现状**：核心逻辑可读，但输入校验、业务逻辑、宿主 API 调用耦合。
- **建议**：
  1. 维持“输入预处理 → 校验 → 执行”的三段式结构；
  2. 常量集中定义（长度、编号上限）；
  3. 宿主对象访问前统一做 guard-check。

### 3.2 性能提升
- **现状**：脚本为小规模循环，性能瓶颈不明显。
- **建议**：
  1. 保持循环内逻辑轻量，避免重复查找；
  2. 对大批量图层场景，可引入更细粒度日志统计和耗时提示。

### 3.3 可维护性增强
- 增加函数职责单一化（`sanitize`、`validate`、`execute` 分离）。
- 使用明确常量名，减少 magic number。
- 将颜色映射与输入映射统一集中在单一对象中。

### 3.4 错误处理完善
- 在批处理入口使用 `try/catch`；
- 在 AE 使用 `try/finally` 保证 undo 组关闭；
- 对“无选中图层/无活动合成”提供明确用户提示。

### 3.5 注释规范
- 注释应优先解释“为什么这样做”，避免与代码重复。
- 对输入校验规则给出简短说明，方便后续维护者扩展。

---

## 4. 已落地实现方案（含步骤与代码指引）

### 4.1 Photoshop 脚本

#### 实施步骤
1. 增加输入边界常量：最大图层名长度、最大起始编号；
2. 引入 `sanitizeBaseName` 清理空白与控制字符；
3. 引入 `isValidInput` 统一验证规则（名称、编号、格式、总长度）；
4. 在执行入口先检查选中图层数量；
5. 在批处理执行入口增加异常捕获；
6. 颜色设置优先按图层 `id` 定位，降低重名图层误匹配概率。

#### 关键实现点
- `numberFormat` 限定为 `^0+$`，避免任意字符导致命名混乱；
- 对超长 `newName` 做截断兜底；
- 统一错误提示提高可诊断性。

### 4.2 After Effects 脚本

#### 实施步骤
1. 增加 `sanitizePrefix`，清理控制字符并限制长度；
2. 应用逻辑前校验 `activeItem instanceof CompItem`；
3. 对未选择图层、空前缀分别告警；
4. 使用 `try/finally` 包裹批处理，确保 `endUndoGroup` 一定执行；
5. 明确“无颜色”时应重置标签为 `0`。

#### 关键实现点
- 避免错误上下文直接访问 `selectedLayers`；
- 出错时给予具体错误信息，提升可维护性与用户可操作性。

---

## 5. 进一步建议（后续迭代）

1. **新增 dry-run 模式**：先预览将产生的名称，不立即写入。
2. **新增冲突策略**：当目标名称已存在时支持“跳过 / 覆盖 / 自动追加后缀”。
3. **增强文档**：在 README 中补充适用版本、输入约束、已知限制。
4. **兼容性矩阵**：标注 PS/AE 不同版本 API 差异与测试结果。

---

## 6. 合规与最佳实践对齐

本次改动遵循以下实践：
- 最小权限/最小影响原则：仅在合法上下文执行改名动作；
- 输入先清洗后验证：降低异常输入导致的问题；
- 失败可观测：统一错误提示与中止逻辑；
- 可维护性优先：常量、函数分层、注释与结构化报告。
